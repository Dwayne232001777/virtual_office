import {
  init_helperFunctions
} from "./chunk-VR6S6BQH.js";
import {
  ShaderStore,
  init_shaderStore
} from "./chunk-4OYBTWIA.js";
import {
  __esm
} from "./chunk-V4OQ3NZ2.js";

// node_modules/@babylonjs/core/Shaders/extractHighlights.fragment.js
var name, shader, extractHighlightsPixelShader;
var init_extractHighlights_fragment = __esm({
  "node_modules/@babylonjs/core/Shaders/extractHighlights.fragment.js"() {
    init_shaderStore();
    init_helperFunctions();
    name = "extractHighlightsPixelShader";
    shader = `#include<helperFunctions>
varying vec2 vUV;uniform sampler2D textureSampler;uniform float threshold;uniform float exposure;
#define CUSTOM_FRAGMENT_DEFINITIONS
void main(void) 
{gl_FragColor=texture2D(textureSampler,vUV);float luma=dot(LuminanceEncodeApprox,gl_FragColor.rgb*exposure);gl_FragColor.rgb=step(threshold,luma)*gl_FragColor.rgb;}`;
    if (!ShaderStore.ShadersStore[name]) {
      ShaderStore.ShadersStore[name] = shader;
    }
    extractHighlightsPixelShader = { name, shader };
  }
});

export {
  extractHighlightsPixelShader,
  init_extractHighlights_fragment
};
//# sourceMappingURL=chunk-C7VMEB45.js.map
